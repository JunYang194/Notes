# ECharts

```js
$echarts.init(document.getElementById('chart'))  > 创建 echarts 的实例对象
chart.prototype.setOption()  > 添加 chart 的数据
chart.prototype.dispose()    > 清除已绑定的 chart
chart.prototype.resize()     > 重新调整 chart 的位置
```

